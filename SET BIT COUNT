#include<iostream>
using namespace std;
int main() 
{
	int q;
	cin>>q;
	while(q--)
	{
	    int n,count=0;
	    cin>>n;
	    while(n!=0)
	    {count = count + (n&1);
	    n=n>>1;}
	    cout<<count<<"\n";
	}
	return 0;
}
